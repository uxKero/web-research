# Model access via the Vercel AI Gateway.
# Locally: paste a key here (copy this file to .env.local).
# On Vercel: set it on the project, or run `vercel link` to use OIDC automatically.
AI_GATEWAY_API_KEY=
